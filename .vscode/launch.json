{
    "version": "0.2.0",
    "configurations": [
        {
            "name": ".NET Core Attach to Service",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:pickProcess}"
        }
    ]
}